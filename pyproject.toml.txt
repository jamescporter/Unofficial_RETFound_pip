[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "retfound-unofficial-pkg" #todo: Change to a globally unique name for PyPI
version = "0.1.0"
authors = [
  { name="Author Name", email="author@example.com" }, #todo: Update with your details
]
description = "An unofficial package for the RETFound foundation model."
readme = "README.md" #todo: Create a brief README.md in the root directory
requires-python = ">=3.13"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
dependencies = [
    "torch==2.5.1",
    "torchvision==0.20.1",
    "timm>=0.9.0",
    "numpy",
    "pandas",
    "scipy",
    "scikit-learn"
]

[tool.setuptools.packages.find]
where = ["."]
include = ["retfound", "retfound.*"]